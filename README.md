# CSS Box Model Profile Page

A styled semantic HTML profile page demonstrating CSS box model principles including margin, padding, border, and layout.  
Created for **Task 2 of ELWEB**, this project builds on semantic structure and introduces visual styling using external CSS.

---

## 📁 Project Structure

- `index.html` — Semantic HTML profile layout
- `style.css` — External stylesheet applying box model styling
- `profile.jpg` — Optional profile image (add your own)

---

## ✨ Features

- Semantic HTML tags: `<header>`, `<nav>`, `<section>`, `<footer>`
- CSS box model: margin, padding, border, content
- Class-based styling for scalability and reuse
- Internal navigation with anchor links
- Visual hierarchy through styled headings, lists, and sections
- Responsive layout foundation using spacing and alignment

---

## 🚀 How to Run

1. Clone the repository:
   ```bash
   git clone https://github.com/jyoshnavi414/css-box-model-profile.git
# css-box-model-profile

